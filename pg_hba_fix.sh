set -e
# ????? ???????? ???? pg_hba.conf
HBA=$(psql -U freshanon -tAc "show hba_file")
echo "pg_hba.conf = $HBA"

# ???? ?????? ??? ??? ? ???????? ?? ?????? ???????
grep -q "host all all all scram-sha-256" "$HBA" || sed -i '1ihost all all all scram-sha-256' "$HBA"

# ??????????? ?????? ? ???????? ???? ????? (?????? ??????)
psql -U freshanon -c "select pg_reload_conf()"
head -n 10 "$HBA"
